{
  "name": "ts_check",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check-types": "ts-node simple-type-checker.ts",
    "extract": "echo 'DEPRECATED: Use TypeSidecar (src/sidecar) instead' && ts-node _legacy/extract-type-definitions.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "TypeScript type checking utilities for Carrick. Type extraction is now handled by the TypeSidecar (src/sidecar).",
  "dependencies": {
    "ts-morph": "^25.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
