{
  "name": "@carrick/type-sidecar",
  "version": "0.1.0",
  "description": "TypeScript type extraction sidecar for carrick",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test --experimental-test-coverage dist/test/**/*.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ts-morph": "^25.0.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "private": true
}
